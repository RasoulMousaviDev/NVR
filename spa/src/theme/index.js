import { preset } from "./preset";

export default {
    preset,
    options: {
        darkModeSelector: '.dark',
        cssLayer: {
            name: 'primevue',
            order: 'theme, base, primevue'
        }
    }
};